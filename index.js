const isURL = url => {};

const getUrlProtocol = url => {};

const getTopLevelDomain = url => {};

const getSubdomain = url => {};

module.exports = {
  isURL,
  getUrlProtocol,
  getTopLevelDomain,
  getSubdomain
};
